version: '3'

services:
  web:
    build: 
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    depends_on:
      - mongodb
    environment:
      MONGO_URI: "mongodb://mongodb:27018/books"

  mongodb:
    image: mongo
    ports:
      - "27018:27018"

  test:
    build:
      context: .
      dockerfile: Dockerfile.test
    depends_on:
      - mongodb
    environment:
      MONGO_URI: "mongodb://mongodb:27018/books"

volumes:
  mongo_data:

